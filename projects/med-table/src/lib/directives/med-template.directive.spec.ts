import { MedTemplateDirective } from './med-template.directive';

describe('MedTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new MedTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
