export const APP_SELECTOR = 'med-table';

export const SELECTOR_AFTER_WEB_COMPONENT = 'div'; // web component doesn't have a height
export const FOOTER_SELECTOR = 'body footer';

export const TABLE_SELECTOR = `${APP_SELECTOR} p-table ${SELECTOR_AFTER_WEB_COMPONENT}`;
export const PAGINATOR_SELECTOR = `${APP_SELECTOR} p-paginator ${SELECTOR_AFTER_WEB_COMPONENT}`;
export const UP_SCROLL_SELECTOR = `${APP_SELECTOR} .med-table__up-scroll`;
